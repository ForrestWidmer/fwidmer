<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Forrest Widmer" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="top-bar">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "Forrest Widmer", root_path %></a></h1>
        </li>
      </ul>
      <section class="top-bar-section">
        <ul class="right">
          <li class="divider"></li>
          <li><%= link_to "About Me", welcome_about_path %></li>
          <li class="divider"></li>
          <li><%= link_to "Current Project", welcome_updates_path %></li>
          <li class="divider"></li>
          <li><%= link_to "Hire Me", contact_new_path %></li>
        </ul>
      </section>
    </nav>

    <% if flash[:notice] %>
        <div data-alert class="alert-box success">
          <a href="#" class="close">&times;</a>
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:error] %>
        <div data-alert class="alert-box warning">
          <a href="#" class="close">&times;</a>
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert] %>
        <div data-alert class="alert-box info">
          <a href="#" class="close">&times;</a>
          <%= flash[:notice] %>
        </div>
      <% end %>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>